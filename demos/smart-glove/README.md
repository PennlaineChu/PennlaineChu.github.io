# Smart Magnifying Glove — tracker

Static pages: open **`concept.html`** for the full concept, **`index.html`** for the prototype 1 lab (checklist, BOM, diary, photos; data stays in the browser).

File layout and long-form technical notes: **[project.md](project.md)**.

`js/shell.js` powers the left sidebar: one section (sub-page) visible at a time; URLs use hashes (e.g. `index.html#timeline`).
